package net.hydrogen2oxygen.tools;

public class PostAndTagsMain {

   public static void main(String[] args) {

      // First collect all files inside a source folder

      // Extract the tags and store them in memory (provide enough for your instance with VM parameters!)

      // Start generating the target html files

      // Generate a protocol also as a html file inside the folder for protocol

   }
}
